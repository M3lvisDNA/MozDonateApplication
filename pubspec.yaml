name: flutter_application_1_flutterdemo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.9

  firebase_auth: ^4.17.3
  firebase_core: ^2.25.3
  cupertino_icons: ^1.0.6
  country_icons: ^3.0.0
  cloud_firestore: ^4.15.4


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
